{
  "name": "@coreforge/types",
  "version": "0.1.0",
  "private": true,
  "description": "Global shared types and generics for CoreForge",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  }
}
