# @coreforge/types

CoreForge framework-wide TypeScript types, generic helpers, and primitives.

## Allowed Dependencies

- None. This package sits at the bottom of the dependency graph and must remain stateless.
