# Module Scaffolding Template

This directory stores boilerplate class setups for implementing new modular extensions.

## Requirements

- Implements the standard `Module` contract.
- Handles DI registration tokens.
- Defines state setup mappings.
