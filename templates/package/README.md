# Package Boilerplate Scaffolding Template

This directory stores template config files for generating new monorepo packages.

## Requirements

- Extended strict compiler base config (`tsconfig.json`).
- Preconfigured script commands (`build`, `clean`).
- Clean root export redirection `index.ts`.
